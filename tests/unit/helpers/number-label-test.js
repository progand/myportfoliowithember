import { numberLabel } from '../../../helpers/number-label';
import { module, test } from 'qunit';

module('Unit | Helper | number label');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = numberLabel(42);
  assert.ok(result);
});
